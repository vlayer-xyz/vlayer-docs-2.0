---
title: Troubleshooting
---

## Server-side proving stuck for more than 1 minute

If proving hangs, check if your HTTP request includes a `keep-alive` header.

**Problematic headers to remove:**
```http
Connection: keep-alive
Keep-Alive: timeout=5, max=100
```

Remove it from the payload before proving and try again.